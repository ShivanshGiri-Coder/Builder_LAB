// Supabase generated types (tables, Row/Insert/Update) — replace after running Supabase CLI gen types.

export type Json = string | number | boolean | null | { [key: string]: Json | undefined } | Json[];
